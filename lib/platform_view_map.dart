import 'package:flutter/material.dart';

class PlatfromViewMap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const UiKitView(viewType: 'platform-map');
  }
}
